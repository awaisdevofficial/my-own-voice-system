# Add this inside your server { } block for https://resonaai.duckdns.org
# so that /livekit/rtc/v1/validate and the WebSocket work (fixes 404).
# LiveKit listens on 7880; adjust proxy_pass if your LiveKit port is different.

location /livekit/ {
    proxy_pass http://127.0.0.1:7880/;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 86400;
}
